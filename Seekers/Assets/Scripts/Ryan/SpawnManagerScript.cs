using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using Random = UnityEngine.Random;

public class SpawnManagerScript : MonoBehaviour
{
    //[Range(0, 10)]
    //public uint hthKitSpawnLmt;
    //[Range(0, 10)]
    //public uint bumperSpawnLmt;
    //[Range(0, 10)]
    //public uint spdBstSpawnLmt;

    //private uint globalHthKitCount;
    //private uint globalBumperCount;
    //private uint globalSpdBstCount;

    //private SpawnerScript spawnerScript;


    // Use this for initialization
    void Awake()
	{

    }
	
	// Update is called once per frame
	void Update ()
	{
		
	}

    private void GetChildSpawnItem()
    {
        //for (int i = 0; i < gameObject.transform.childCount; i++)
        //{
        //    get access to each child
        //    Transform childObject = gameObject.transform.GetChild(i);


        //    spawnerScript = childObject.GetComponent<SpawnerScript>();

        //    spawnerScript.Setup();

        //    gameObject.transform.GetComponentInChildren;
        //}
        
    }
}
